FROM python:3.11-slim

RUN useradd -r -s /bin/bash noezema
WORKDIR /workspace

RUN apt-get update && apt-get install -y --no-install-recommends \
    bash coreutils curl && \
    rm -rf /var/lib/apt/lists/*

USER noezema
